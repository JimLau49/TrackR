import * as React from "react";
import {
    Image,
    Platform,
    StyleSheet,
    Text,
    TouchableOpacity,
    View
  } from "react-native";

export default function addMeal({navigation}) {
    return (
        <Text>
            Hello world
        </Text>
    )
}